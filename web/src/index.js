import React from 'react'; 
import ReactDOM from 'react-dom'; // O react consegue ver a arvore de elementos da nossa aplicação com HTML.
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (Javascript + HTML)